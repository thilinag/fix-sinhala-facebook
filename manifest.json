{
  "manifest_version": 3,
  "name": "Fix Sinhala rendering in facebook",
  "description": "Adds a temporary fix to add custom css to facebook.com to fix an issue with zwj not rendering correctly in facebook.",
  "version": "1.0",
  "content_scripts": [
    {
      "matches": ["https://*.facebook.com/*"],
      "css": ["fix-sinhala.css"]
    }
  ]
}
